export const classNames = (...classes) => classes.filter(Boolean).join(' ');

export const styles = {
    focusRing: "focus:ring-1 focus:outline-none focus:ring-rounded-sm focus:ring-gray-300",
    focusBorder: "focus:border-gray-600 focus:outline-none",
    disabled: "disabled:text-gray-400 disabled:bg-white disabled:border-gray-200"
};

