export const classNames = (...classes) => classes.filter(Boolean).join(' ');

export const styles = {
    focusRing: "focus:ring-1 focus:outline-none focus:ring-rounded-sm focus:ring-gray-800",
    focusBorder: "focus:border-gray-800 focus:outline-none",
    disabled: "disabled:text-gray-300 disabled:bg-white disabled:border-gray-200"
};

